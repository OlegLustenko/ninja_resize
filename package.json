{
  "name": "node-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker-compose -f ./dockerfiles/docker-compose-dev.yml up",
    "prod": "docker-compose -f ./dockerfiles/docker-compose.yml up",
    "build": "yarn prod -- --build",
    "lint-staged": "lint-staged"
  },
  "lint-staged": {
    "sso/src/**/*.js": "eslint",
    "frontend/src/**/*.js": "eslint",
    "resize/src/**/*.js": "eslint",
    "mailer/src/**/*.js": "eslint",
    "load-balancer/src/**/*.js": "eslint"
  },
  "pre-commit": "lint-staged",
  "keywords": [],
  "author": "Oleg Lustenko",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "lint-staged": "^3.4.1"
  },
  "devDependencies": {
    "pre-commit": "^1.2.2"
  }
}
